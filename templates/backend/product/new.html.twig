{% extends 'base.html.twig' %}

{% block content %}
    <h1> Ajoutez votre nouveau produit</h1>
    {{ form_start(form_Product) }}
    {{ form_widget(form_Product) }}
    <button class="btn btn-danger">{{ button_label|default('Save') }}</button>
    {{ form_end(form_Product) }}

{% endblock %}
